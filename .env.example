# DeepSeek-OCR Service Configuration

# Optional API key for authentication
# Leave empty for no authentication
API_KEY=

# Model settings (advanced users only)
# Default model - do not change unless you know what you're doing
# DS_OCR_MODEL=deepseek-ai/DeepSeek-OCR

# Server port (default: 8765 on host, 8000 in container)
# Change if port 8765 is already in use on your host
# HOST_PORT=8765

# Enable GPU support (requires NVIDIA GPU + nvidia-docker)
# Uncomment the deploy section in docker-compose.yml to enable
# USE_GPU=false

# ============================================
# LLM Provider Configuration (for text tasks)
# ============================================

# LLM Provider: "ollama" (FREE local) or "openai" (paid cloud)
LLM_PROVIDER=ollama

# Ollama settings (when LLM_PROVIDER=ollama)
OLLAMA_URL=http://localhost:11434
LLM_MODEL=qwen2.5:7b

# OpenAI settings (when LLM_PROVIDER=openai)
# OPENAI_API_KEY=sk-your-key-here

# ============================================
# Security
# ============================================

# Service-to-service API key (required for protected endpoints)
SERVICE_API_KEY=your-secret-key-here
